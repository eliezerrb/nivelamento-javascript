// Dataset do elemento é um obj que vai ter todos os atributos que foi definido no HTML com data- alguma coisa

// Selecionando por atributo, tbem funciona no CSS
const card1 = document.querySelector('[data-product-id="32"]');

console.log(card1);
